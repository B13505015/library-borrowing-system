# Legacy backend module notice

`library-system-backend` is no longer the primary backend runtime.

Please use `library-api/library-api` for API development and deployment.

This folder is temporarily kept for:
- JSON importer helpers under `importer/`
- manual test utilities (`Test*.java`)

Do not add new business logic here.
